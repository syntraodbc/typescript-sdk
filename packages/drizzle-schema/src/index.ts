// Placeholder entry point.
//
// The real contents of this file will be generated at build time from
// `schemas/quickbooks.json` at the monorepo root. Until the codegen is wired
// up, this file exports a version marker so downstream consumers can detect
// early-access builds.

export const SYNTRA_SDK_VERSION = '0.0.0-alpha';
