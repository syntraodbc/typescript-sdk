#!/usr/bin/env node
// Placeholder CLI entry point for @syntraodbc/codegen.
//
// The real CLI will accept --host, --port, --user, --database, --out and
// emit a typed Drizzle schema by introspecting a live Syntra ODBC instance.
// For now it prints a banner and exits.

console.log('syntra-codegen: early access. CLI not implemented yet.');
console.log('See https://github.com/syntraodbc/typescript-sdk for progress.');
process.exit(0);
